import React from 'react';

export function UserOrders({ orders }) {
  return (
    <section className='user=orders'>
      <h3>Orders</h3>
      TODO: Orders table
      {/* <ul>
        {orders.map((order, idx) => (
          <li key={`o${idx}`}>{order.status}</li>
        ))}
      </ul> */}
    </section>
  );
}
